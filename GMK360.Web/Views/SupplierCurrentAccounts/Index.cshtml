@model IEnumerable<GMK360.Core.Entities.Finance.SupplierCurrentAccount>
@{
    ViewData["Title"] = "Tedarik�i Cari Hesaplar�";
    Layout = "~/Views/Shared/_ConstructionLayout.cshtml";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h2 class="h4 mb-1">Tedarik�i Cari Hesaplar�</h2>
        <p class="text-muted mb-0">Malzeme al�nan firmalar�n finansal bakiye ve bor� durumu</p>
    </div>
</div>

<div class="card shadow-sm border-0">
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th class="ps-4">Tedarik�i / Firma</th>
                        <th>�leti�im</th>
                        <th class="text-end">G�ncel Bakiye (Borcumuz)</th>
                        <th class="text-end pe-4">��lemler</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var account in Model)
                    {
                        <tr>
                            <td class="ps-4 fw-bold">@account.PhonebookContact.Name</td>
                            <td>
                                <div class="small">@account.PhonebookContact.Name</div>
                                <div class="text-muted small"><i class="bi bi-telephone-fill me-1"></i>@account.PhonebookContact.PhoneNumber</div>
                            </td>
                            <td class="text-end fw-bold @(account.CurrentBalance > 0 ? "text-danger" : "text-success")">
                                ₺ @account.CurrentBalance.ToString("N2")
                            </td>
                            <td class="text-end pe-4">
                                <a href="/SupplierCurrentAccounts/Details/@account.Id" class="btn btn-sm btn-primary">
                                    <i class="bi bi-journal-text me-1"></i> Ekstre & �deme
                                </a>
                            </td>
                        </tr>
                    }
                    @if (!Model.Any())
                    {
                        <tr>
                            <td colspan="4" class="text-center py-4 text-muted">Kay�tl� cari hesap bulunmuyor. Sat�n alma yap�ld�k�a veya �deme girildik�e hesaplar otomatik a��lacakt�r.</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>





