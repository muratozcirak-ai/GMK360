@{
    ViewData["Title"] = "Şantiye ERP Özeti";
    Layout = "_ConstructionLayout";

    var activeProjects = ViewBag.ActiveProjects ?? 0;
    var delayedPhases = ViewBag.DelayedPhases ?? 0;
    var totalPayments = ViewBag.TotalPaymentsThisMonth ?? 0m;
}

<div class="container-fluid py-4">
    <!-- FİRMA BAŞLIĞI -->
    <div class="d-flex align-items-center mb-4 p-3 bg-white shadow-sm rounded-4 border border-light">
        <div class="bg-primary bg-opacity-10 p-3 rounded-3 me-3 text-primary">
            <i class="bi bi-buildings-fill fs-2"></i>
        </div>
        <div>
            <h3 class="fw-bold mb-0">@((ViewBag.Agency != null) ? ViewBag.Agency.CompanyName : "İnşaat Firması")</h3>
            <p class="text-muted mb-0 small">
                @if(ViewBag.Agency != null)
                {
                    <span><i class="bi bi-building"></i> Vergi No: @ViewBag.Agency.TaxNumber</span>
                }
                else
                {
                    <span>Firma bilgileri güncellenmemiş.</span>
                }
            </p>
        </div>
    </div>

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h4 class="fw-bold mb-0"><i class="bi bi-speedometer2 text-primary me-2"></i> Şirket Özeti (Dashboard)</h4>
        <a href="/ConstructionProject/Create" class="btn btn-primary rounded-pill px-4 shadow-sm">
            <i class="bi bi-plus-lg me-1"></i> Yeni Şantiye Başlat
        </a>
    </div>

    <!-- İSTATİSTİK KARTLARI -->
    <div class="row g-4 mb-4">
        <div class="col-md-4">
            <div class="card border-0 shadow-sm rounded-4 text-center p-4 h-100">
                <i class="bi bi-cone-striped display-5 text-primary mb-3"></i>
                <h2 class="fw-bold text-dark mb-0">@activeProjects</h2>
                <p class="text-muted mb-0 fw-semibold">Devam Eden Aktif Şantiye</p>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card border-0 shadow-sm rounded-4 text-center p-4 h-100">
                <i class="bi bi-exclamation-triangle display-5 text-warning mb-3"></i>
                <h2 class="fw-bold text-dark mb-0">@delayedPhases</h2>
                <p class="text-muted mb-0 fw-semibold">Geciken Taşeron Aşaması</p>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card border-0 shadow-sm rounded-4 text-center p-4 h-100 bg-primary text-white">
                <i class="bi bi-wallet2 display-5 mb-3 opacity-75"></i>
                <h3 class="fw-bold mb-0 mt-2">₺ @totalPayments.ToString("N0")</h3>
                <p class="mb-0 mt-2 opacity-75">Bu Ayki Onaylı Hakediş (Gider)</p>
            </div>
        </div>
    </div>

    <div class="row g-4">
        <!-- TEKLİF & GÖRÜŞÜLEN PROJELER -->
        <div class="col-md-8">
            <div class="card shadow-sm border-0 rounded-4 h-100">
                <div class="card-header bg-white border-bottom-0 pt-4 px-4 d-flex justify-content-between align-items-center">
                    <h5 class="fw-bold mb-0"><i class="bi bi-megaphone me-2 text-warning"></i> Aday / Görüşmesi Süren Projeler</h5>
                    <a href="/ConstructionProject/Index?status=teklif" class="btn btn-sm btn-outline-secondary rounded-pill">Tümünü Gör</a>
                </div>
                <div class="card-body p-4">
                    @if (ViewBag.TeklifProjeler != null && ((IEnumerable<GMK360.Core.Entities.Construction.ConstructionProject>)ViewBag.TeklifProjeler).Any())
                    {
                        <div class="table-responsive">
                            <table class="table table-hover align-middle mb-0">
                                <thead class="table-light">
                                    <tr>
                                        <th>Proje Adı</th>
                                        <th>Lokasyon</th>
                                        <th>Tarih</th>
                                        <th>Durum</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var teklif in (IEnumerable<GMK360.Core.Entities.Construction.ConstructionProject>)ViewBag.TeklifProjeler)
                                    {
                                        <tr>
                                            <td class="fw-bold text-primary">@teklif.Name</td>
                                            <td class="text-muted small">
                                                <i class="bi bi-geo-alt me-1"></i>
                                                @(string.IsNullOrEmpty(teklif.Address) ? "Belirtilmedi" : (teklif.Address.Length > 20 ? teklif.Address.Substring(0,20)+"..." : teklif.Address))
                                            </td>
                                            <td class="small">@teklif.CreatedAt.ToString("dd.MM.yyyy")</td>
                                            <td><span class="badge bg-warning text-dark px-3 py-2 rounded-pill">Sözleşme Bekliyor</span></td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    }
                    else
                    {
                        <div class="text-center py-5 text-muted bg-light rounded-3">
                            <i class="bi bi-inbox fs-1 d-block mb-3 opacity-50"></i>
                            <p class="mb-0">Şu an görüşmesi (teklif) devam eden bir proje bulunmuyor.</p>
                        </div>
                    }
                </div>
            </div>
        </div>

        <!-- HIZLI ERİŞİM KARTLARI -->
        <div class="col-md-4">
            <div class="card shadow-sm border-0 rounded-4 bg-light text-center p-5 h-100 d-flex flex-column justify-content-center">
                <i class="bi bi-folder-check display-4 text-muted mb-3 opacity-50"></i>
                <h5 class="fw-bold">Belge Arşivi</h5>
                <p class="text-muted small mb-4">Tüm şantiyelerinize ait CAD planları, sözleşmeler ve iskan belgeleri tek bir yerde güvende.</p>
                <a href="/DocumentArchive/Index?context=construction" class="btn btn-dark rounded-pill shadow-sm px-4">Arşivi Aç</a>
            </div>
        </div>
    </div>
</div>
